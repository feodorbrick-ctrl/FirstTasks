import React from 'react';

const Error = () => {
    return (
            <h1 style={{color: 'red', width: '85%'}}>
                you have navigate to a non-existent page!
            </h1>
    );
};

export default Error;